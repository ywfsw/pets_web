# ----------------------------------------------------------------
# JetBrains IDE (WebStorm, IntelliJ)
# ----------------------------------------------------------------
# 这是最重要的！忽略你所有的 WebStorm 个人配置
.idea/

# IntelliJ / WebStorm 模块文件 (如果有的话)
*.iml


# ----------------------------------------------------------------
# 操作系统 (macOS, Windows, Linux)
# ----------------------------------------------------------------
# 忽略 macOS 自动生成的 .DS_Store 文件
.DS_Store

# 忽略 Windows 缩略图缓存
Thumbs.db
desktop.ini

# 忽略 Linux/Vim 交换文件
*~


# ----------------------------------------------------------------
# Node.js / 前端项目 (未来保障)
# ----------------------------------------------------------------
#
# 即使你现在用的是 CDN, 你未来也可能会用 npm,
# 提前加上这些规则是最佳实践。

# 1. 忽略所有依赖包 (这是最重要的规则)
node_modules/

# 2. 忽略日志文件
*.log
npm-debug.log*

# 3. 忽略构建产物 (如果你未来用了 Vue CLI 或 Vite)
dist/
build/
.output/

# 4. 忽略环境变量文件 (绝对不能提交你的 API 密钥或密码!)
.env
.env.local
.env.*.local